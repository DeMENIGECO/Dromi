# non mette nella zip questo stesso file, non serve
.gitattributes export-ignore

# non mette nella zip il README, sarà sicuramente giá letto
README.md export-ignore

# non mette nella zip la cartella .github, che serve solo a GitHub
.github/ export-ignore
